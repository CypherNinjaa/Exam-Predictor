# ===========================================
# AI EXAM PREDICTOR - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values

# ============ DATABASE (Prisma) ============
# PostgreSQL connection string
# For local: postgresql://user:password@localhost:5432/exam_predictor
# For Railway: Will be auto-provided as DATABASE_URL
DATABASE_URL="postgresql://postgres:password@localhost:5432/exam_predictor?schema=public"

# ============ GEMINI AI ============
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY="your_gemini_api_key_here"

# Enable Gemini 3.0 Preview (Public Preview - Released Nov 18, 2025)
# ⚠️ WARNING: Limited to ~30-50 requests/day on free tier
# ⚠️ More expensive on paid tiers
# ⚠️ May be less stable (preview version)
# Default: false (uses stable Gemini 2.5 Pro)
# Set to "true" only for testing or low-traffic scenarios
ENABLE_GEMINI_3="false"

# ============ CHROMADB ============
# Path for local vector database storage
CHROMADB_PATH="./data/chromadb"

# ============ APP CONFIG ============
NEXTAUTH_SECRET="your_nextauth_secret_here"
NEXTAUTH_URL="http://localhost:3000"

# For Railway deployment, set this to your Railway URL:
# NEXTAUTH_URL="https://your-app.up.railway.app"

# ============ COLLEGE CONFIG ============
DEFAULT_COLLEGE="Amity University Patna"
DEFAULT_COLLEGE_CODE="AUP"

# ============ ENVIRONMENT ============
NODE_ENV="development"

# ============ RAILWAY SPECIFIC ============
# These are auto-provided by Railway when you add PostgreSQL
# PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE
